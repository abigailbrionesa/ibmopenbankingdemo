# Vault Configuration

This directory contains HashiCorp Vault configuration for secrets management.

## Purpose

Manages secure storage of:
- API keys and secrets
- Database credentials
- OAuth client secrets
- Encryption keys
- TLS certificates

## Structure

Vault configurations will include policy definitions and initialization scripts.

**Note:** No actual secrets are stored in this repository. All secrets are managed through Vault at runtime.